h1
  | Таблица пользователей
.buttons_panel
  = button_to "Добавить пользователя", new_admin_user_path, method: :get, class: "btn btn-dark"
section#users
  table.table.table-bordered
    thead.thead-dark
      tr
        th[scope="col"]
          | #
        th[scope="col"]
          | action
        th[scope="col"]
          | id
        th[scope="col"]
          | admin?
        th[scope="col"]
          | Дата регистрации
        th[scope="col"]
          | Имя
        th[scope="col"]
          | Фамилия
        th[scope="col"]
          | Username
        th[scope="col"]
          | e-mail
    tbody
      - @users.each do |user|
        tr
          th[scope="row"]
          td
            = button_to "Уничтожить", admin_user_path(user.id),  method: :delete, confirm: 'Are You Sure?'
            = button_to "Редактировать", edit_admin_user_path(user.id), method: :get, confirm: 'Are You Sure?'
          td
            = user.id
          td
            = user.admin
          td
            = user.created_at
          td
            = user.first_name
          td
            = user.last_name
          td
            = user.username
          td
            = user.email
